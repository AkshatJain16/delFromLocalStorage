Created A Form, that adds data when submitted to the end of the form. It stores the data in local storage with unique key as email. 
A delete button is also added with data, upon clicking it, It deletes the data from the screen and the local storage
